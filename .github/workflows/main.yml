name: Main Promotion
on:
  push:
    branches:
      - main

jobs:
  promote:
    name: Promote To Main
    runs-on: ubuntu-latest
    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
          
      - name: Promote Image
        run: |
          # Use the commit SHA from the PR that was just merged
          # We look at the parent of the merge commit
          SOURCE_SHA=$(git rev-parse HEAD^2) 

          docker buildx imagetools create \
            --tag ${{ secrets.DOCKER_USERNAME }}/main:latest \
            --tag ${{ secrets.DOCKER_USERNAME }}/main:${{ github.sha }} \
            ${{ secrets.DOCKER_USERNAME }}/mr:$SOURCE_SHA